for(shift = 0 ... 140)
	dayOfTheWeek = shiftTODayOfTheWeek();
	dayNursesNeeded = 0;
	nightNursesNeeded = 1;
	
	[] nursesForThePattern
	
	if(isWeekend(dayOfTheWeek)){
		dayNursesNeeded = 9;
	}
	
	else{
		dayNursesNeeded = 6;
	}

	while(nursesToAssign > 0)
		nurseId = drawNurse();
		patternId = drawPattern();
		
		nursesForThePattern.add(nurseId, patternId);
		
		
		
		//0 - rest; 1 - day ; 2 - night
		accordingToPattern = patterns[patternId][dayOfTheWeek]; 
		
		if(isNightShift){
			if(accordingToPattern == 2){
				if(constraint.checkSchedule(nurseId, shift, schedule))
					setSchedule
			}
			
		}
		
		else{
			if(accordingToPattern == 0){
				//to nic nie rób
			}
	
			if(accordingToPattern == 1){
				if(constraint.checkSchedule(nurseId, shift, schedule))
					setSchedule
			}
		
		}
	
		
	}
